fastapi==0.68.0
uvicorn==0.15.0
python-dotenv==0.19.0
httpx==0.24.1
# Minimal stable versions - avoid all typing conflicts
# FastAPI 0.68.0 has built-in pydantic that works

# NowPayments integration dependencies
requests==2.31.0      # Additional HTTP client support
cryptography==41.0.7  # For webhook signature verification
certifi==2023.11.17
requests-toolbelt==1.0.0
python-multipart==0.0.6
email-validator==2.1.0  # Email validation for Pydantic EmailStr

# Supabase client with compatible httpx version
supabase==2.1.0