fastapi==0.88.0
uvicorn==0.20.0
python-dotenv==1.0.0
pydantic==1.10.13
httpx==0.24.1
python-multipart==0.0.6
# Using older versions to avoid Rust compilation
# Pydantic v1 doesn't require Rust, v2 does