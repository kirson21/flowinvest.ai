# RUST-FREE Procfile for Flow Invest

# Backend Service - Using minimal server and rust-free requirements
web: cd backend && pip install -r requirements-rust-free.txt && python server_minimal.py

# Alternative: If you want to try the full server with fallbacks
# web: cd backend && python server.py

# Frontend Service (if needed as separate service)
# frontend: cd frontend && yarn start